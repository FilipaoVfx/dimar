# Supabase (client-side, exposed to the browser)
# Configure these in Vercel → Project Settings → Environment Variables
PUBLIC_SUPABASE_URL=https://su-proyecto.supabase.co
PUBLIC_SUPABASE_ANON_KEY=su-anon-key
